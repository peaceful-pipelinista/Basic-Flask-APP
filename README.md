# Basic-Flask-APP


1. I created a flask app. 
2. It has /api route.
3. Data is stored in data.json file (backend file)
4. When /api route is accessed, the app reads data from the file and     returns it as a response.
5. I used Flask’s jsonify() function to return data as JSON response